# --- core numerics & data frames ---------------------------------numpy==1.26.4pandas==2.2.1scipy==1.12.0               # required by many ML libsjoblib==1.3.2               # model persistence / parallel helpers# --- tabular ML: gradient-boosted trees --------------------------lightgbm==4.3.0xgboost==2.0.3              # uses numpy 1.26+ wheelscatboost==1.2.3             # CPU wheels; GPU build optional# --- neural networks --------------------------------------------tensorflow==2.16.1           # brings in keras-core 3.x by default# If you prefer the standalone Keras 3 API:# keras==3.3.3               # uncomment & pin if needed# --- hyper-parameter optimisation -------------------------------optuna==3.6.1hyperopt==0.2.7scikit-optimize==0.9.0# --- experiment orchestration -----------------------------------papermill==2.5.0            # head-less notebook executionpyyaml==6.0.1               # load search-space config.ymlclick==8.1.7                # lightweight CLIs for pipeline scriptstqdm==4.66.2                # neat progress bars# --- plotting / reporting ---------------------------------------matplotlib==3.8.4plotly==5.19.0seaborn==0.13.2             # optional, nice statistical styles# --- misc --------------------------------------------------------scikit-learn==1.4.2         # metrics, train/test split, etc.